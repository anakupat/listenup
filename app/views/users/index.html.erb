<h1>Users</h1>

<table style="color: white" class="table">
  <% @users.each do |user| %>
    <tr>
      <td>
          <%= user.username %> <%= link_to "Add Friend", friendships_path(:friend_id => user.id), :method => :post %>
       </td>
    </tr>
  <% end %>
</table>

